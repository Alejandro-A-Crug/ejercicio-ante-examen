name: Docker CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - src/**
  pull_request:
    branches: [ main ]
    paths:
      - src/**
# Mirame, soy un cambio
jobs:
  build:
    name: build and push Docker image
    runs-on: ubuntu-latest
    steps: 
      # 1 
      - uses: actions/checkout@v4
      # 2  
      - name: Login to DockerHub
        uses: docker/login-action@v3 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }} # esto se puede cambiar
          password: ${{ secrets.DOCKERHUB_TOKEN }} # esto se puede cambiar
      # 3   
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true 
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/galeria:latest # esto se puede cambiar
  aws:
    name: Deploy image to aws
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    # 1
    - name: copy docker compose via ssh key  
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.AWS_HOSTNAME }} # esto se puede cambiar, secret --> IP de la maquina
        username: ${{ secrets.AWS_USERNAME }} # esto se puede cambiar, secret --> admin
        port: 22 # ${{ secrets.PORT }}
        key: ${{ secrets.AWS_PRIVATEKEY }} # esto se puede cambiar, VOCKEY 
        source: "docker-compose.yml"
        target: /home/admin
    # 2
    - name: script deploy docker services 
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.AWS_HOSTNAME }} # esto se puede cambiar, secret
        username: ${{ secrets.AWS_USERNAME }} # esto se puede cambiar, secret
        key: ${{ secrets.AWS_PRIVATEKEY }} # esto se puede cambiar, VOCKEY
        port: 22  # ${{ secrets.PORT }}
        script: |
            sleep 60
            docker compose down --rmi all
            docker compose up -d